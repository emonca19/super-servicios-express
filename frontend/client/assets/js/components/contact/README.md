contact component

Archivos:
- `contact-component.js` — componente y manejo del DOM.
- `logic.js` — funciones puras reutilizables (validaciones, normalizaciones).
- `index.js` — punto de exportación.

Consejo: importar utilidades desde `./logic.js` para pruebas unitarias. Mantener el DOM en `contact-component.js`.
