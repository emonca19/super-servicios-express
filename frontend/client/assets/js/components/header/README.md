header-component

- `header-component.js` — registra el custom element y maneja DOM.
- `header-component.template.js` — template HTML.
- `logic.js` — transformación de datos para el header (nav items, links).
- `index.js` — barrel.

Beneficio: pruebas de la lógica sin necesidad de montar el DOM.
