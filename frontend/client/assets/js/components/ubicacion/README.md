ubicacion component

- `ubicacion-component.js` — logic + DOM.
- `logic.js` — utilidades para construir URLs de mapas y dar formato a direcciones.
- `index.js` — barrel para import.

Mover utilidades a `logic.js` facilita testeo y reutilización entre componentes.
