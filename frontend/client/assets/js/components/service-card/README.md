service-card

- `service-card.js` — componente principal.
- `logic.js` — utilidades para formateo (precio, título).
- `index.js` — barrel para importación.

Consejo: usa `logic.js` para normalizar datos antes de renderizar en el template.
