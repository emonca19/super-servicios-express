Features subcomponent

- `features.component.js` — componente que muestra características.
- `features.data.js` — datos por defecto.
- `logic.js` — normalización y ordenamiento de features.
- `index.js` — barrel (ya existe).

Usa `logic.js` para preparar los datos antes de pasar al template.
