sse-input

- `index.js` — barrel re-exportando `../sse-input.js`.
- `logic.js` — helpers para sanitizar y validar valores antes de pasarlos al DOM.

Sugerencia: cuando se actualice la implementación principal, moverla a esta carpeta y actualizar el barrel.
