sse-button

- `index.js` — barrel que re-exporta la implementación existente (`../sse-button.js`).
- `logic.js` — funciones puras para normalizar etiquetas o atributos del botón.

Nota: La implementación original permanece en `frontend/shared/components/sse-button.js` para compatibilidad.
